<xr-scene ar-system="modes:Marker" bind:ready="handleReady">
  <xr-assets bind:loaded="handleAssetsLoaded">
    <xr-asset-load wx:for="{{modelsAndRecognize}}" type="gltf" asset-id="{{item.id}}" src="{{item.model}}" />
  </xr-assets>
  <xr-node wx:if="{{loaded}}">
    <xr-ar-tracker wx:for="{{modelsAndRecognize}}" mode="Marker" src="{{item.ai_image}}">
      <xr-env env-data="xr-frame-team-workspace-day" />
      <xr-gltf node-id="{{item.id}}" model="{{item.id}}" rotation="270 0 0" scale="0.24 0.24 0.24" id="{{item.id}}" mesh-shape bind:touch-shape="handleTouchModel" />
    </xr-ar-tracker>
  </xr-node>
  <xr-camera clear-color="0.4 0.8 0.6 1" background="ar" is-ar-camera />
</xr-scene>