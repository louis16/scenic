<!--index.wxml-->
<view class="container">
  <map longitude="103.33938" latitude="29.547287" scale="15" max-scale="17" min-scale="13" markers="{{markers}}" style="width: 100vw;height: 100vh;" enable-satellite bindmarkertap="{{marktap}}" bindtap="maptap">
  </map>
  <FunctinonBar bind:clickItem="handleFuncClick" />
  <!-- <flicker/> -->
  <!-- 任务 -->
  <block wx:if="{{showTaskModal}}">
    <TaskList bind:closeModal="closeModal" title="任务1">
      <view slot="header" style="height: 10vh;background-color: red;">
        <text>头部</text>
      </view>
      <accordion slot="body" />
    </TaskList>
  </block>
  <!-- 背包 -->
  <block wx:if="{{showPackageModal}}">
    <TaskList bind:closeModal="closeModal" title="背包">
      <view slot="header" style="height: 10vh;background-color: red;">
        <text>背包头部</text>
      </view>
      <accordion slot="body" />
    </TaskList>
  </block>
</view>