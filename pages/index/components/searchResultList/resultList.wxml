<view class="resultWrap {{list.length === 0 && 'opcacity'}}" style="top: {{navHeight + 54}}px;">
  <view class="resultItem" bindtap="itemClick" data-item="{{item}}" style="{{index === 0 && 'border-top:none'}}" wx:for="{{list}}" wx:key="index">
    <text>{{item.name}}</text>
  </view>
</view>